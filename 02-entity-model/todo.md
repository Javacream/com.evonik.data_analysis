## review presentation
* actions
  * open the file person.erd in Visual Studio Code
  * press CTRL-O to open the diagram view
* check
  * you see a model entity person

## training
* create a new file publishing.erd
* create entity Book with isbn (string), title (string), pages (int), price (double), available (boolean)